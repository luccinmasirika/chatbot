import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  direction: 'rtl',
  palette: {
    background: {
      paper: '#FDFDF7',
    },
    primary: {
      main: '#323740',
      dark: '#221D1D',
    },
    secondary: {
      main: '#FFF',
    },
  },
});
